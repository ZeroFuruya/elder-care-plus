# ElderCare+ AI service — local development
#
# Copy to `.env` (git-ignored) and fill in a local-only value. Never commit a real
# secret; the deployed Space reads its secret from the Space's own secret settings.
ELDERCARE_AI_SHARED_SECRET=
ELDERCARE_AI_MAX_UPLOAD_BYTES=8388608
