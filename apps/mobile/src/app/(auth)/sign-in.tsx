import { ScreenScaffold } from '@/components/screen-scaffold';

/** Email/password sign in. Session refresh and password reset come with Sprint 1. */
export default function SignInScreen() {
  return (
    <ScreenScaffold title="Sign in" description="Email and password fields, plus password reset." />
  );
}
